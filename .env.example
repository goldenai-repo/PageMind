# Copy to .env.local (or configure in Doppler) and fill in from Firebase Console

# Client SDK — Firebase Console → Project settings → Your apps → Web app config
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
# Exact bucket id from Firebase Storage (no gs:// prefix), e.g. project.firebasestorage.app
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# Admin SDK — Firebase Console → Project settings → Service accounts → Generate new private key
FIREBASE_ADMIN_PROJECT_ID=
FIREBASE_ADMIN_CLIENT_EMAIL=
FIREBASE_ADMIN_PRIVATE_KEY=

# Google Books — overlay Summary lookup (server only)
# Google Cloud Console → APIs & Services → Enable "Books API" → Credentials → API key
GOOGLE_BOOKS_API_KEY=

# Firebase Console setup:
# 1. Enable Email/Password and Google under Authentication → Sign-in method
# 2. Add "localhost" to Authentication → Settings → Authorized domains
# 3. For Google OAuth, the Firebase-provided OAuth client is used automatically
